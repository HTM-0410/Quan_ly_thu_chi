# TIP-000: Chuẩn hóa Baseline Migration và Môi trường Test Cô lập

## HEADER
- **TIP-ID:** TIP-000
- **V1-Task:** V1-00 (F26)
- **REQ-ID:** REQ-026
- **Priority:** P0 (Hỗ trợ)
- **Depends on:** D09 (Đã phê duyệt)
- **Deliverables:**
  - `Quan_ly_thu_chi/supabase/migrations/20260810000001_m14_global_categories_and_rpcs_baseline.sql`
  - `docs/develop/v1/MIGRATION_MANIFEST.md`
  - Cập nhật `Quan_ly_thu_chi/web/src/test/helpers.ts` để chặn ghi Supabase demo từ xa khi chạy test mặc định.
  - Sửa mock trong `Quan_ly_thu_chi/web/src/components/PaymentModal.test.tsx`.
  - Đồng bộ validation số tiền 0 trong `Quan_ly_thu_chi/web/src/lib/ocrSchema.ts` và `ocrSchema.test.ts`.

## CONTEXT & PHẠM VI
- **Thư mục làm việc:** `D:/Quản lý thu chi` và `Quan_ly_thu_chi/web`
- **Tài liệu tham chiếu:** `spec_v1.md` (§5 V1-00), `BLUEPRINT.md`, `PRODUCT_UX_AUDIT_2026-09-07.md` (F26)
- **Hiện trạng:**
  - Có 2 thư mục migration: `Quan_ly_thu_chi/supabase/migrations` (17 files) và `supabase/migrations` (4 files m9–m12).
  - Thiếu DDL `CREATE TABLE global_categories` (chỉ có ALTER TABLE) và thiếu RPC `mark_debt_paid`.
  - `npm test` chạy `vitest run` tự động thực thi `api-debt.test.ts` và `debt-flow.test.ts`, kết nối và ghi dữ liệu vào Supabase demo từ xa.
  - `PaymentModal.test.tsx` fail 9/10 tests vì thiếu mock `listAccounts` và `createManualTransaction`.
  - `ocrSchema.test.ts` fail 1 test do schema cho phép 0 trong khi test expect reject.

## INVARIANTS KHÔNG ĐƯỢC VI PHẠM
1. Lệnh test mặc định (`npm test` hoặc `vitest run`) KHÔNG ĐƯỢC PHÉP gửi bất kỳ request mạng nào ghi/xóa vào database remote demo Supabase.
2. Không sửa assertion một cách hời hợt hoặc bỏ test chỉ để tạo kết quả xanh giả tạo.
3. Không làm hỏng các migrations hiện có.

## ACCEPTANCE CRITERIA
- **AC-000-1 (Manifest & Migrations đầy đủ):**
  - Given thư mục migrations,
  - When đối chiếu với toàn bộ 27 RPC và các bảng trong `api.ts`,
  - Then tất cả các bảng (gồm `global_categories`) và RPC (gồm `mark_debt_paid`) đều có định nghĩa DDL/SQL hoàn chỉnh và có thứ tự áp dụng rõ ràng trong `MIGRATION_MANIFEST.md`.
- **AC-000-2 (Cô lập Test Suite mặc định):**
  - Given developer chạy `npm test` hoặc `vitest run` ở môi trường local offline,
  - When test suite khởi chạy,
  - Then không có bất kỳ request mạng nào được gửi tới Supabase demo (`kldtrthnslpdhqrwlglg.supabase.co`); các integration test remote chỉ chạy khi có cờ môi trường tường minh `RUN_REMOTE_TESTS=true`.
- **AC-000-3 (PaymentModal Tests Green):**
  - Given `PaymentModal.test.tsx`,
  - When mock `listAccounts` và `createManualTransaction` được cấu hình đầy đủ,
  - Then 10/10 test cases của `PaymentModal.test.tsx` đều PASS.
- **AC-000-4 (OCR Schema Zero Validation Thống nhất):**
  - Given Zod schema và business rules,
  - When parse một giao dịch hoàn chỉnh để ghi sổ,
  - Then số tiền phải là số nguyên dương (`amount_minor > 0`); nếu `amount_minor <= 0` thì schema từ chối với thông báo rõ ràng; test case `ocrSchema.test.ts` PASS.
- **AC-000-5 (Typecheck & Unit Test Health):**
  - Given toàn bộ codebase sau khi chỉnh sửa,
  - When chạy `npm run typecheck` và các bài unit test cô lập,
  - Then không có lỗi type (`tsc --noEmit` exit code 0) và tất cả bài test cô lập đều PASS.

## BUILDER TEST PLAN
- Kiểm tra file DDL migration mới bằng mắt và đối chiếu cú pháp PostgreSQL.
- Chạy `npx vitest run src/components/PaymentModal.test.tsx` → 10/10 pass.
- Chạy `npx vitest run src/lib/ocrSchema.test.ts` → toàn bộ pass.
- Chạy `npx vitest run src/lib/format.test.ts src/lib/daily.test.ts src/lib/billOcr.test.ts` → toàn bộ pass.
- Chạy `npm run typecheck` → 0 lỗi.

## CONTRACTOR VERIFICATION PLAN
- Đọc diff thực tế của `helpers.ts`, `PaymentModal.test.tsx`, `ocrSchema.ts`.
- Chạy kiểm tra độc lập các file test và xác nhận không có network request ra ngoài.
- Kiểm tra nội dung `MIGRATION_MANIFEST.md` và migration SQL mới.
