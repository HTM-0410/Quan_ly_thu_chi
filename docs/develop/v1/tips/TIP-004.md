# LỆNH THI CÔNG: TIP-004

- **Mã TIP:** TIP-004
- **Mã V1:** V1-04 (Báo Cáo Toàn Diện Server-side & Đồng Bộ Bộ Lọc)
- **Mã Lỗi Khắc Phục:** F06, F20
- **Người phát lệnh:** THẦU (Contractor)
- **Người thi công:** THỢ (Builder)
- **Thời gian phát lệnh:** 2026-09-07T16:00:00+07:00

---

## 1. Mục tiêu và Phạm vi
Khắc phục các lỗi sai lệch số liệu và bất đồng bộ khoảng thời gian trong Báo cáo tài chính (`ReportsPage.tsx`):
1. **F06 (Tổng hợp danh mục đầy đủ server-side):**
   - Loại bỏ việc query chỉ 500 dòng giao dịch phía client rồi cộng tay.
   - Thêm RPC `get_category_expenses_breakdown` để tổng hợp toàn bộ chi tiêu theo danh mục ở database, bảo đảm chính xác 100% dù có hàng chục nghìn giao dịch.
   - Hỗ trợ đầy đủ nhóm "Chưa phân loại" (`__uncategorized__`) cho các chi tiêu chưa gắn danh mục.
   - Gom các danh mục nhỏ ngoài Top 7 vào nhóm "Khác" (`__other__`), bảo đảm tổng của tất cả các phần trong Pie Chart và bảng chi tiết bằng đúng 100% tổng chi tiêu.
   - Popup chi tiết khi click vào lát cắt danh mục hiển thị đầy đủ các giao dịch tương ứng (kể cả Chưa phân loại và Khác).
2. **F20 (Đồng bộ bộ lọc thời gian toàn trang):**
   - Đồng bộ khoảng ngày được chọn cho: Thẻ KPI (Thu / Chi / Chênh lệch), Biểu đồ cột xu hướng (Bar Chart), Biểu đồ tròn tỷ trọng (Pie Chart).
   - Chuẩn hóa nhãn preset: "Tháng này", "12 tháng gần nhất", "4 quý gần nhất", "5 năm gần nhất", "Tùy chỉnh".
   - Hiển thị khoảng ngày hiệu lực trực quan bên dưới thanh preset: `Hiệu lực: DD/MM/YYYY – DD/MM/YYYY`.
   - Tự động đồng bộ tháng xem Heatmap theo khoảng thời gian được chọn.

---

## 2. Các Bước Thực Hiện Cụ Thể (Builder Tasks)

### Bước 1: Migration Database
Tạo migration `Quan_ly_thu_chi/supabase/migrations/20260810000008_category_expenses_breakdown_rpc.sql` và đăng ký vào `MIGRATION_MANIFEST.md` (#27):
- Tạo hàm RPC `get_category_expenses_breakdown(p_start_date, p_end_date, p_timezone)`:
  - Tính tổng chi tiêu theo danh mục (`user` category, `global` category, `__uncategorized__`).
  - Lọc trạng thái `posted`, loại chi tiêu `expense`, loại trừ vốn nợ `is_debt_principal: true`.
  - Quy đổi múi giờ chính xác.

### Bước 2: Cập nhật API Layer & Types
- `web/src/lib/database.types.ts`: Thêm định nghĩa kiểu cho RPC `get_category_expenses_breakdown`.
- `web/src/lib/api.ts`: Thêm hàm `getCategoryExpensesBreakdown(opts)` với cơ chế fallback client-side an toàn.

### Bước 3: Nâng cấp Giao diện `ReportsPage.tsx`
- Đồng bộ bộ lọc thời gian: Pie Chart dùng chung `range.start` và `range.end` với KPI và Bar Chart.
- Chuẩn hóa nhãn `PRESET_LABELS` và hiển thị khoảng ngày hiệu lực.
- Xử lý nhóm "Chưa phân loại" và nhóm "Khác" trong Pie Chart và danh sách.
- Modal drill-down hiển thị giao dịch theo danh mục được chọn trong đúng khoảng thời gian của bộ lọc.

### Bước 4: Viết Test & Kiểm thử
- Viết test suite cho `ReportsPage`: kiểm tra đồng bộ khoảng thời gian, nhóm chưa phân loại/khác, drill-down modal, tổng hợp dữ liệu.
- Chạy `npm test` và `npm run typecheck`.

---

## 3. Tiêu chí Nghiệm thu (Acceptance Criteria)
- **AC-04.1:** Biểu đồ tròn (Pie Chart) và thẻ KPI cùng dùng chung khoảng thời gian đã chọn (`range.start` đến `range.end`), không bị lệch về tháng hiện tại khi xem Quý hay Năm.
- **AC-04.2:** Các khoản chi tiêu chưa gắn danh mục hiển thị rõ ràng dưới mục "Chưa phân loại", không bị âm thầm loại bỏ khỏi tỷ trọng.
- **AC-04.3:** Các danh mục ngoài Top 7 được gom vào mục "Khác", tổng tỷ trọng của tất cả các phần luôn đạt đúng 100% tổng chi tiêu.
- **AC-04.4:** Nhãn các preset thời gian rõ nghĩa ("12 tháng gần nhất", "4 quý gần nhất", "5 năm gần nhất") và hiển thị khoảng ngày hiệu lực.
- **AC-04.5:** 100% tests pass offline, `tsc --noEmit` 0 lỗi.
